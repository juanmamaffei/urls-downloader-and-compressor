require 'spec_helper'

describe FilesCompressor do
  # Before, create a petition.
  # Then, create two subfiles in tmp/petition_id folder.
  # Then, create a zip file in tmp folder with petition_id.zip name.

end
